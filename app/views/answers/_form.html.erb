<%= form_with(model: answer, url: { action: "create" }) do |form| %>
  <% if answer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(answer.errors.count, "error") %> prohibited this answer from being saved:</h2>

      <ul>
        <% answer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :question_answer, style: "display: block" %>
    <%= form.text_area :question_answer %>
  </div>

  <div>
    <%= form.label :correct, style: "display: block" %>
    <%= form.check_box :correct %>
  </div>

  <div>
  <%= form.submit "create", class: "btn btn-primary my-3" %>
  </div>  </div>
<% end %>
