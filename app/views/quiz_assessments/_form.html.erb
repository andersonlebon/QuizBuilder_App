<%= form_with(model: quiz_assessment) do |form| %>
  <% if quiz_assessment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(quiz_assessment.errors.count, "error") %> prohibited this quiz_assessment from being saved:</h2>

      <ul>
        <% quiz_assessment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :question_description, style: "display: block" %>
    <%= form.text_area :question_description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
